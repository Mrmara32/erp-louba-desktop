pywebview==6.2.1
pyinstaller==6.21.0

# --- Ajouts pour le mode offline (serveur Django local + synchro) ---
Django==6.0.7
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
dj-database-url==3.1.2
whitenoise==6.12.0
waitress==3.0.2
requests==2.32.3
# pdf2image RETIRÉ : vérifié inutilisé dans tout le code (paie/pdf.py génère
# les PDF en pur Python via reportlab). Aucun binaire Poppler à embarquer.
reportlab==5.0.0
pillow==12.3.0
PyJWT==2.13.0
# psycopg2-binary n'est PAS nécessaire hors-ligne (SQLite uniquement) — ne
# pas l'inclure évite d'alourdir l'exécutable et ses dépendances système.
